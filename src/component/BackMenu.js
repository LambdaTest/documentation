import React from 'react';
import { Link } from 'docusaurus/router';

const BackToMainMenu = () => {
  return (
    <Link to="support" className="back-to-main-menu">
      Back to Main Menu
    </Link>
  );
};

export default BackMenu;
