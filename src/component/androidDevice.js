const React = require('react');

export default function AndroidDeviceTag({ value, color, bgColor }) {
    return (
        <span className='androidDeviceColor'>
            {value}
        </span>
    )
}
